[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Optional: nicer paths if you ever want them.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
